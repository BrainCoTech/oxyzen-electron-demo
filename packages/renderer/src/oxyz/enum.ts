enum CONNECTIVITY { connecting, connected, disconnecting, disconnected }
enum CONTACT_STATE { unknown, off, eeg, all }
enum ORIENTATION { unknown, normal, upsideDown }

export { CONNECTIVITY, CONTACT_STATE, ORIENTATION }

